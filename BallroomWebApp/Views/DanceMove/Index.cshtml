@model IEnumerable<BallroomWebApp.Models.DanceMove>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Video)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Syllabus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Video.DanceVideoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Syllabus.SyllabusId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DanceMoveId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DanceMoveId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DanceMoveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
